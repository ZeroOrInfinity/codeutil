            // [columnComment]
            if(searchMap.get("[column2]")!=null && !"".equals(searchMap.get("[column2]"))){
                criteria.andLike("[column]","%"+searchMap.get("[column2]")+"%");
            }